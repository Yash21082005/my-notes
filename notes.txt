ProblemDetails problem = ProblemDetails.forNotFound();
problem.setTitle("404");
problem.setDetail(
    "Appointment with LAN " + request.getAgreementNo() + " not found"
);

throw new BrakeAppException(problem);
