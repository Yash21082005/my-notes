if (decodedCombinedBytes.length < GCM_IV_LENGTH) {

    ProblemDetails problem = ProblemDetails.forBadRequest();
    problem.setDetail("Received data is too short to contain IV.");
    problem.setTitle("400");

    throw new BrakeAppException(problem);
}
