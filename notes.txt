I am running this Spring Boot project on my local machine.

Redis is NOT installed locally, so every API fails with:

org.springframework.data.redis.RedisConnectionFailureException:
Cannot get Jedis connection

I want a LOCAL-ONLY solution.

Requirements:

1. Analyze the project and locate all Redis configuration classes (RedisConfig, CacheManager, LettuceConnectionFactory/JedisConnectionFactory, RedisTemplate, etc.).

2. Disable Redis ONLY when the "local" Spring profile is active.

3. Do NOT remove Redis from the project.
   Do NOT delete any code.
   Do NOT affect dev, QA, UAT or production environments.

4. Add the minimum required changes so that:
   - Local profile starts without Redis.
   - Redis beans are not created in local.
   - Services that use Redis do not fail.
   - If Redis is unavailable, bypass cache and directly use the database.
   - Keep all existing Redis logic for non-local profiles.

5. Update application-local.properties (or application-local.yml) if required.

6. If any service directly injects RedisTemplate/Jedis/CacheManager and fails because Redis is disabled, modify that service so Redis becomes optional in local profile and database is used as fallback.

7. Explain every file you changed and why.

Do NOT suggest installing Redis.
Do NOT remove dependencies from pom.xml.
Implement the complete local-only solution automatically.
