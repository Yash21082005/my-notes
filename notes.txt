catch (JsonProcessingException e) {
    log.error("Error converting map to JSON string: " + e.getMessage());

    ProblemDetails problem = ProblemDetails.forInternalError();
    problem.setDetail("Failed to generate JSON for DMS request");
    problem.setTitle("500");

    throw new BrakeAppException(problem);
}
