Act as a Senior Spring Boot architect.

Do NOT suggest generic fixes.
Do NOT remove @Primary.
Do NOT disable my routing datasource implementation.
Do NOT replace my routing datasource with a single datasource.

My routing implementation using BrakeRoutingDataSource, BrakeDataSourceContext, BrakeDataSourceType, leaderDataSource(), replicaDataSource() and routingDataSource(@Primary) is intentional and must remain.

Current startup error:

No bean named 'entityManagerFactory' available

Your task is to inspect the ENTIRE project and find the REAL root cause instead of guessing.

Specifically:

1. Find why Spring Boot is NOT creating the entityManagerFactory bean.
2. Search the entire project for:
   - @EnableJpaRepositories
   - @EnableRedisRepositories
   - LocalContainerEntityManagerFactoryBean
   - EntityManagerFactory
   - JpaTransactionManager
   - PlatformTransactionManager
   - EntityManagerFactoryBuilder
   - @SpringBootApplication
   - @EnableAutoConfiguration
   - exclude=
   - DataSourceConfig
3. Check whether any custom configuration is disabling Spring Boot's JPA auto configuration.
4. Check whether any repository is referencing entityManagerFactory by name.
5. Check whether AddressRedisRepository is actually a Redis repository or is incorrectly being treated as a JPA repository.
6. Check for bean name conflicts caused by my custom DataSourceConfig.
7. Preserve @Primary on routingDataSource().
8. Modify only the required files so that Spring Boot creates entityManagerFactory correctly while keeping the routing datasource architecture intact.

IMPORTANT:
If you think @Primary should be removed, first prove with code why it is the root cause. If you cannot prove it, DO NOT remove it.

Output:
- Root cause
- File causing the problem
- Exact code change
- Why that fixes entityManagerFactory
- Apply the fix automatically.
